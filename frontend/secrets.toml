# .streamlit/secrets.toml
# Add this file locally (never commit it). On Streamlit Community Cloud,
# paste these values into the Secrets section of your app settings.

BACKEND_URL = "https://your-backend-url.railway.app"
